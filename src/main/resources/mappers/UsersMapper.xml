<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.liangchen.demo1.mapper.UsersMapper">
    <select id="selectById" parameterType="Integer" resultType="site.liangchen.demo1.pojo.Users">
        SELECT * from users where id=#{id}
    </select>
</mapper>
